applications:
    app:
        stack:
            - lisp:2.3
            - sbcl:2.3
        web:
            commands:
                start: sbcl --load system.lisp --eval '(main)'
            locations:
                "/":
                    root: "public"
                    allow: true


services:
    db:
        type: postgresql:15


routes:
    "https://{default}/":
        type: upstream
        upstream: "app"