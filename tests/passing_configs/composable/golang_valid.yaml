applications:
    app:
        stack:
            - golang:1.21
        web:
            commands:
                start: ./bin/app
            locations:
                "/":
                    root: "public"
                    allow: true

services:
    db:
        type: postgresql:15

routes:
    "https://{default}/":
        type: upstream
        upstream: "app"