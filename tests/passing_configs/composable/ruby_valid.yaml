applications:
    app:
        stack:
            - ruby:3.3
        web:
            commands:
                start: bundle exec puma -C config/puma.rb
            locations:
                "/":
                    root: "public"
                    passthru: true
                    allow: true


services:
    db:
        type: postgresql:15


routes:
    "https://{default}/":
        type: upstream
        upstream: "app"