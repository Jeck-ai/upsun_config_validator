applications:
    app:
        type: java@17
        web:
            commands:
                start: java -jar application.jar
            locations:
                "/":
                    root: "public"
                    allow: true

services:
    db:
        type: postgresql:15

routes:
    "https://{default}/":
        type: upstream
        upstream: "app"