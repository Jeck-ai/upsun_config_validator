applications:
    app:
        type: nodejs:20
        hooks:
            build: |
                npm install
        web:
            commands:
                start: npm start

services:
    db:
        type: mongodb:5.0

routes:
    "https://{default}/":
        type: upstream
        upstream: "app" 