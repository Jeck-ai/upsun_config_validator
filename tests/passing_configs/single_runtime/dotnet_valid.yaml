applications:
    app:
        type: dotnet:8.0
        web:
            commands:
                start: dotnet application.dll
            locations:
                "/":
                    root: ""
                    allow: true


services:
    db:
        type: postgresql:15


routes:
    "https://{default}/":
        type: upstream
        upstream: "app"