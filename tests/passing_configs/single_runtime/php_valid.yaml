applications:
    app:
        type: php:8.2
        build:
            flavor: none
        web:
            locations:
                '/':
                    root: 'public'
                    index:
                        - index.php
                    scripts: true

services:
    db:
        type: mariadb:10.4
    cache:
        type: redis:7.0

routes:
    "https://{default}/":
        type: upstream
        upstream: "app" 