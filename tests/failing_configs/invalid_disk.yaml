applications:
  web:
    type: nodejs@18
    disk: 512  # This should fail
    web:
      commands:
        start: npm start

services:
  db:
    type: postgresql:15
    disk: 2048  # This should fail

routes:
  "https://{default}/":
    type: upstream
    upstream: "web"