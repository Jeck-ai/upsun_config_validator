applications:
  myapp:
    type: "ruby@2.7.0"   # Invalid: uses @ instead of :
services:
  database:
    type: "mariadb:10.5"
routes:
  "https://{default}/":
    type: upstream
    upstream: "myapp:http"