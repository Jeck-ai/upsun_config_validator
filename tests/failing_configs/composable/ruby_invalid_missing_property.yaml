applications:
    app:
        stack:
            - ruby:3.3
        # Missing web configuration section
        disk: 512

services:
    db:
        type: postgresql:15
        disk: 1024

routes:
    "https://{default}/":
        type: upstream
        upstream: "app"