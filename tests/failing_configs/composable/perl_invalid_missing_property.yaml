applications:
    app:
        stack:
            - perl:5.38
        # Missing web configuration
        disk: 512

services:
    db:
        type: mariadb:10.6
        disk: 1024

routes:
    "https://{default}/":
        type: upstream
        upstream: "app"