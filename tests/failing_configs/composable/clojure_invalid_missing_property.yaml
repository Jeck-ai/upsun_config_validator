applications:
    app:
        stack:
            - java:17
            # Missing required language in stack
        web:
            commands:
                start: java -jar app.jar
        disk: 512

services:
    db:
        type: postgresql:15
        disk: 1024

routes:
    "https://{default}/":
        type: upstream
        upstream: "app"