applications:
  myapp:
    type: "nodejs@14.15.1"
services:
  database:
    type: "mariadb:10.5"
routes:
  "example.com/":   # Invalid: missing required http(s):// prefix
    type: redirect
    to: "https://{default}/"